class UserInfo {
    constructor({userNameSelector, userInfoSelector}) {
        
    }

    getUserInfo = (params) => {
        
    }

    setUserInfo = (params) => {
        
    }
    
    
}

export default UserInfo;