import Popup from "./Popup";

class PopupWithImage extends Popup {
    constructor(params) {
        super(params)
    }

    open = (img, src, alt) => {
        //  logic
    }
    
}

export default PopupWithImage;